<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.tomboati">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_new_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_new_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/ThemeTomboAti">
        <activity android:name=".view.activity.SyaratActivity"></activity>
        <activity android:name=".view.activity.SignUpActivity" />
        <activity android:name=".view.activity.SignInActivity" />
        <activity android:name=".view.activity.WisataReligiActivity" />
        <activity android:name=".view.activity.DoaDzikirActivity" />
        <activity android:name=".view.activity.SholatActivity" />
        <activity android:name=".view.activity.UmrohHajiActivity" />
        <activity android:name=".view.activity.MainActivity" />
        <activity android:name=".view.activity.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>